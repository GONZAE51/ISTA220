using System;

namespace Exercise_1
{
    class Program
    {
        static void Main(string[] args)
        {
            //tell the user what he is doing
            Console.WriteLine("We will calculate the area and circumference of a circle and volume of a hemisphere: ");

            //Find the radius
            Console.WriteLine("Insert the radius: ");
            string radius = Console.ReadLine();
            double radius1 = double.Parse(radius);
            //  multiple the radius by 2 and PI  and to find area multiply pi by the rad * rad
            double circumference = radius1 * 2 * Math.PI;
            double area = radius1 * radius1 * Math.PI;
            double volume = (4 / 3 * Math.PI * Math.Pow(radius1, 3)) / 2;

            Console.WriteLine($"Radius is {radius} and circumference is {circumference} and the area is {area} and the volume is {volume} ");
            //tell the user what he is doing
            Console.WriteLine("                                                       ");
            Console.WriteLine("We will find the area of a triangle given three sides");

            // plug in to the equation what Ax^2,Bx,C are
            Console.WriteLine(" side a length: ");
            string sidea = Console.ReadLine();
            double sa = double.Parse(sidea);
            Console.WriteLine(" side b length");
            string sideb = Console.ReadLine();
            double sb = double.Parse(sideb);
            Console.WriteLine(" side c length");
            string sidec = Console.ReadLine();
            double sc = double.Parse(sidec);
            // Solve for P
            double HalfCir = (sa + sb + sc) / 2;
            double TriangleArea = Math.Sqrt(HalfCir) * (HalfCir - sa) *(HalfCir - sb) *(HalfCir - sc);
            Console.WriteLine($" P is {HalfCir} Area {TriangleArea}");

            //Solve for quadratic equation
            Console.WriteLine("Solve for the quadratic equation ");

            string Q1 = Console.ReadLine();
            string Q2 = Console.ReadLine();
            string Q3 = Console.ReadLine();
            int A = int.Parse(Q1);
            int B = int.Parse(Q2);
            int C = int.Parse(Q3);
            //Solve for X

            double Quad1X = (-B + Math.Sqrt((B * B) - (4 * A * C)) / (2 * A));
            double Quad2X = (-B - Math.Sqrt((B * B) - (4 * A * C)) / (2 * A));

            Console.WriteLine($"quad is the positive outcome {Quad1X} ");
            Console.WriteLine($"quad2 is the negative outcome {Quad2X} ");
           

            
            
             
                
            


            

            

            



            









            
        }
    }
}
